Hash: 散列将一个任意的长度通过hash函数算法 转换成一个固定值。 移位。
Map:  理解成地图 x，y存储。

------------------------------------
    1。HashMap中的key可以为null；会把null当成一个key来存储；
    2。put方法中 如果hash键重复冲突的情况下：
        会把当前值赋予给老的值；
        当新的值赋予给当前值；
        并返回老的值。
        2.1是否会覆盖的值？--不会覆盖。
    3。hashMap什么时候做扩容的？
        --put的时候 超过容器四分三的时候会扩容。
        --以2的倍数去扩增容量。
        --源码扩容方法：transfer
    4。最终存储的地方，数据结构：哈希+链表

    5. 伸缩性角度（不足之处）：****
        --每当hashMap扩容的时候，需要重新去add entry对象， 需要重新hash。 然后放入我们新的entry table数组中。
        --如果在工作中明确知道hashmap存储多少值，最好先指定他们的大小， 防止在put时候进行再次扩容很多次。
        --时间复杂度-跟hash算法决定。

